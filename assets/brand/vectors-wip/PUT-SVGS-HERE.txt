Drop the rough brand SVGs in this directory.

Nothing generates from them and nothing imports them — they sit here until they
match the concept art exactly, which is roadmap item P11. See ../README.md for
what is wrong with the ones supplied so far and the recommended way to fix it.

Preferred names (anything recognisable is fine, they get normalised on the way
in): mark.svg, wordmark-primary.svg, wordmark-medium.svg, wordmark-small.svg

This placeholder goes away once real files land.
